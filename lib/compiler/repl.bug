(function write (x)
    (impl:write-file *standard-output* x))

(function repl ()
  (while t
    (write "b>> ")
    (let ((expr (read *standard-input*))) 
      (let ((code (compile expr)))
        (print (call code))
        (write "\n")))))